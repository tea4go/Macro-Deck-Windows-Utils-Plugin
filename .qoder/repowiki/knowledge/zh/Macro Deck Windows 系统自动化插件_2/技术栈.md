- **框架**：.NET 10.0 Windows Forms (`UseWindowsForms=true`)。
- **依赖**：`H.InputSimulator` (热键/输入模拟), `Newtonsoft.Json` (配置序列化), `System.Drawing.Common` (图标处理)。
- **宿主集成**：引用 `Macro Deck 2.dll` (API v40)，通过 `libs/` 或安装路径动态链接。