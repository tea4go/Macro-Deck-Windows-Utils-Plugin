- **插件生命周期管理**：`Main.cs` 作为唯一入口继承 `MacroDeckPlugin`，在 `Enable()` 阶段集中实例化并注册所有 `PluginAction` 子类至宿主框架。
- **全局状态共享**：通过 `PluginInstance` 静态类暴露主实例，确保 `InputSimulator` 等核心工具在多个动作间共享且保持单例状态。
- **配置与执行解耦**：动作逻辑（Actions）与配置界面（GUI/Views）通过 `GetActionConfigControl` 契约动态关联，由宿主框架负责触发与渲染。